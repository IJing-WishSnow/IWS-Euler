apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketdata
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.marketdata.replicas }}
  selector:
    matchLabels:
      app: marketdata
  template:
    metadata:
      labels:
        app: marketdata
    spec:
      containers:
        - name: marketdata
          image: {{ .Values.marketdata.image }}
          imagePullPolicy: Never
          ports:
            - containerPort: {{ .Values.marketdata.port }}
          env:
            - name: KAFKA_BROKER
              value: "kafka:9092"
          readinessProbe:
            tcpSocket:
              port: {{ .Values.marketdata.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: marketdata
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: marketdata
  ports:
    - port: {{ .Values.marketdata.port }}
      targetPort: {{ .Values.marketdata.port }}
